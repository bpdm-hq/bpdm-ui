node_modules
dist
storybook-static
site
.turbo
.angular
out-tsc
*.log
.DS_Store
.env*

# local-only working notes / scratch docs — never pushed to the (public) repo
_local/
DEVELOPER_NOTES.md

# compodoc output (regenerated by the Angular storybook build)
packages/angular/documentation.json
