// Makes the @testing-library/jest-dom matchers (toBeInTheDocument, etc.) available
// on Vitest's `expect` for the type-checker. Test-only; not part of the bundle.
import "@testing-library/jest-dom/vitest";
