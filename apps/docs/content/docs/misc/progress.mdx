---
title: Progress
description: A process indicator — determinate or indeterminate, five colours, three sizes, in React (@bpdm/ui) and Angular (@bpdm/ng).
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';
import {
  ProgressBasicDemo,
  ProgressLabelDemo,
  ProgressInsideDemo,
  ProgressFormatDemo,
  ProgressSizesDemo,
  ProgressVariantsDemo,
  ProgressIndeterminateDemo,
  ProgressDynamicDemo,
  ProgressInCardDemo,
} from '@/components/progress-demos';
import { Preview } from '@/components/preview';

The **Progress** bar shows how far along a task is — **determinate** (drive
`value`, the fill animates to its width) or **indeterminate** (an animated sweep
when there's no known total). Five colours, three sizes, an optional value/label
row, and accessible by default. In Angular it's `<bpdm-progress-bar>`.

## Usage

<Preview>
  <ProgressBasicDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-usage.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function ProgressUsage() {
  return <ProgressBar value={60} />;
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-usage.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'progress-usage',
  imports: [BpdmProgressBar],
  templateUrl: './progress-usage.html',
})
export class ProgressUsage {}
```

```html title="progress-usage.html"
<bpdm-progress-bar [value]="60" />
```

  </Tab>
</Tabs>

## Value & label

Set `showValue` to show the percentage, and `label` for leading text (e.g.
"Uploading…"). They sit in a row above the bar.

<Preview>
  <ProgressLabelDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-label.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function UploadProgress() {
  return <ProgressBar value={72} showValue label="Uploading…" variant="success" />;
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-label.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'upload-progress',
  imports: [BpdmProgressBar],
  templateUrl: './progress-label.html',
})
export class UploadProgress {}
```

```html title="progress-label.html"
<bpdm-progress-bar [value]="72" showValue label="Uploading…" variant="success" />
```

  </Tab>
</Tabs>

## Value inside the bar

Set `valuePosition="inside"` to render the value within the bar itself — handy
in tight layouts.

<Preview>
  <ProgressInsideDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-inside.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function InsideProgress() {
  return <ProgressBar value={72} valuePosition="inside" />;
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-inside.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'inside-progress',
  imports: [BpdmProgressBar],
  templateUrl: './progress-inside.html',
})
export class InsideProgress {}
```

```html title="progress-inside.html"
<bpdm-progress-bar [value]="72" valuePosition="inside" />
```

  </Tab>
</Tabs>

## Custom format

Pass `format` to replace the percentage with your own label — e.g. a `value/max`
readout. It implies `showValue`.

<Preview>
  <ProgressFormatDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-format.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function StorageBar() {
  return (
    <ProgressBar value={50} max={100} label="Storage" format={(v, max) => `${v}/${max} GB`} />
  );
}
```

  </Tab>
  <Tab value="Angular">

`format` is a function input — define it on the component and bind it.

```ts title="progress-format.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'storage-bar',
  imports: [BpdmProgressBar],
  templateUrl: './progress-format.html',
})
export class StorageBar {
  readonly storageFormat = (v: number, max: number) => `${v}/${max} GB`;
}
```

```html title="progress-format.html"
<bpdm-progress-bar [value]="50" [max]="100" label="Storage" [format]="storageFormat" />
```

  </Tab>
</Tabs>

## Sizes

The `size` prop offers `sm`, `md` (default), and `lg`.

<Preview>
  <ProgressSizesDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-sizes.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function ProgressSizes() {
  return (
    <div className="flex flex-col gap-4">
      <ProgressBar size="sm" value={62} />
      <ProgressBar size="md" value={62} />
      <ProgressBar size="lg" value={62} />
    </div>
  );
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-sizes.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'progress-sizes',
  imports: [BpdmProgressBar],
  templateUrl: './progress-sizes.html',
})
export class ProgressSizes {}
```

```html title="progress-sizes.html"
<bpdm-progress-bar size="sm" [value]="62" />
<bpdm-progress-bar size="md" [value]="62" />
<bpdm-progress-bar size="lg" [value]="62" />
```

  </Tab>
</Tabs>

## Colours

The `variant` prop offers `primary` (default), `success`, `warning`,
`destructive`, and `info`.

<Preview>
  <ProgressVariantsDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-colours.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function ProgressColours() {
  return (
    <div className="flex flex-col gap-3">
      <ProgressBar variant="primary" value={18} showValue label="primary" />
      <ProgressBar variant="success" value={36} showValue label="success" />
      <ProgressBar variant="warning" value={54} showValue label="warning" />
      <ProgressBar variant="destructive" value={72} showValue label="destructive" />
      <ProgressBar variant="info" value={90} showValue label="info" />
    </div>
  );
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-colours.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'progress-colours',
  imports: [BpdmProgressBar],
  templateUrl: './progress-colours.html',
})
export class ProgressColours {}
```

```html title="progress-colours.html"
<bpdm-progress-bar variant="primary" [value]="18" showValue label="primary" />
<bpdm-progress-bar variant="success" [value]="36" showValue label="success" />
<bpdm-progress-bar variant="warning" [value]="54" showValue label="warning" />
<bpdm-progress-bar variant="destructive" [value]="72" showValue label="destructive" />
<bpdm-progress-bar variant="info" [value]="90" showValue label="info" />
```

  </Tab>
</Tabs>

## Indeterminate

When there's no known total, set `indeterminate` for an animated sweep.

<Preview>
  <ProgressIndeterminateDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-indeterminate.tsx"
import { ProgressBar } from '@bpdm/ui/progress';

export function IndeterminateProgress() {
  return <ProgressBar indeterminate />;
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-indeterminate.ts"
import { Component } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'indeterminate-progress',
  imports: [BpdmProgressBar],
  templateUrl: './progress-indeterminate.html',
})
export class IndeterminateProgress {}
```

```html title="progress-indeterminate.html"
<bpdm-progress-bar indeterminate />
```

  </Tab>
</Tabs>

## Dynamic value

Drive `value` from state and the fill animates smoothly to each new width. Switch
the `variant` to `success` when it completes.

<Preview>
  <ProgressDynamicDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-dynamic.tsx"
import { useEffect, useState } from 'react';
import { ProgressBar } from '@bpdm/ui/progress';

export function Syncing() {
  const [value, setValue] = useState(20);
  useEffect(() => {
    const id = setInterval(() => {
      setValue((v) => (v >= 100 ? 0 : Math.min(100, v + 10)));
    }, 700);
    return () => clearInterval(id);
  }, []);

  return (
    <ProgressBar value={value} showValue label="Syncing" variant={value >= 100 ? 'success' : 'primary'} />
  );
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-dynamic.ts"
import { Component, signal } from '@angular/core';
import { BpdmProgressBar } from '@bpdm/ng';

@Component({
  selector: 'syncing',
  imports: [BpdmProgressBar],
  templateUrl: './progress-dynamic.html',
})
export class Syncing {
  readonly value = signal(20);

  constructor() {
    setInterval(() => this.value.update((v) => (v >= 100 ? 0 : Math.min(100, v + 10))), 700);
  }
}
```

```html title="progress-dynamic.html"
<bpdm-progress-bar [value]="value()" showValue label="Syncing"
  [variant]="value() >= 100 ? 'success' : 'primary'" />
```

  </Tab>
</Tabs>

## In a card

A real-world layout — a progress bar inside a file-upload card, climbing on
**Upload** and flipping to `success` at 100%.

<Preview>
  <ProgressInCardDemo />
</Preview>

<Tabs groupId="framework" items={['React', 'Angular']}>
  <Tab value="React">

```tsx title="progress-in-card.tsx"
import { useState } from 'react';
import { ProgressBar } from '@bpdm/ui/progress';
import { Button } from '@bpdm/ui/button';

export function UploadCard() {
  const [value, setValue] = useState(0);
  const [uploading, setUploading] = useState(false);
  const start = () => {
    setUploading(true);
    setValue(0);
    const id = setInterval(() => {
      setValue((v) => {
        if (v >= 100) {
          clearInterval(id);
          setUploading(false);
          return 100;
        }
        return v + 8;
      });
    }, 220);
  };

  return (
    <div className="w-72 space-y-4 rounded-xl border bg-card p-5 shadow-sm">
      <div>
        <p className="text-sm font-medium">report-2025.pdf</p>
        <p className="text-xs text-muted-foreground">4.2 MB</p>
      </div>
      <ProgressBar value={value} showValue variant={value >= 100 ? 'success' : 'primary'} />
      <Button size="sm" variant="secondary" appearance="outline" onClick={start} disabled={uploading}>
        {value >= 100 ? 'Upload again' : 'Upload'}
      </Button>
    </div>
  );
}
```

  </Tab>
  <Tab value="Angular">

```ts title="progress-in-card.ts"
import { Component, signal } from '@angular/core';
import { BpdmProgressBar, BpdmButton } from '@bpdm/ng';

@Component({
  selector: 'upload-card',
  imports: [BpdmProgressBar, BpdmButton],
  templateUrl: './progress-in-card.html',
})
export class UploadCard {
  readonly value = signal(0);
  readonly uploading = signal(false);

  start() {
    this.uploading.set(true);
    this.value.set(0);
    const id = setInterval(() => {
      this.value.update((v) => {
        if (v >= 100) {
          clearInterval(id);
          this.uploading.set(false);
          return 100;
        }
        return v + 8;
      });
    }, 220);
  }
}
```

```html title="progress-in-card.html"
<div class="w-72 space-y-4 rounded-xl border bg-card p-5 shadow-sm">
  <div>
    <p class="text-sm font-medium">report-2025.pdf</p>
    <p class="text-xs text-muted-foreground">4.2 MB</p>
  </div>
  <bpdm-progress-bar [value]="value()" showValue [variant]="value() >= 100 ? 'success' : 'primary'" />
  <button bpdmButton size="sm" variant="secondary" appearance="outline" (click)="start()" [disabled]="uploading()">
    {{ value() >= 100 ? 'Upload again' : 'Upload' }}
  </button>
</div>
```

  </Tab>
</Tabs>

## API

| Prop | Type | Default | Description |
| --- | --- | --- | --- |
| `value` | `number` | `0` | Current value (ignored when `indeterminate`). |
| `max` | `number` | `100` | Maximum value. |
| `indeterminate` | `boolean` | `false` | Animated sweep when there's no known total. |
| `size` | `sm \| md \| lg` | `md` | Track height. |
| `variant` | `primary \| success \| warning \| destructive \| info` | `primary` | Fill colour. |
| `showValue` | `boolean` | `false` | Show the value (defaults to the percentage). |
| `valuePosition` | `outside \| inside` | `outside` | Value in a row above, or inside the bar. |
| `format` | `(value, max) => ReactNode` | — | Custom value label. Implies `showValue`. |
| `label` | `ReactNode` | — | Leading text shown opposite the value. |

In Angular, every prop above is an `@Input` on `<bpdm-progress-bar>` (`format` is
a function input, `label` is a `string`).

## Accessibility

- Renders `role="progressbar"` with `aria-valuemin`, `aria-valuemax`,
  `aria-valuenow`, and `aria-valuetext`, so assistive tech announces progress.
- When `indeterminate`, `aria-valuenow`/`aria-valuemax` are omitted and
  `aria-valuetext` reads "Loading".
- Pass a `label` (or wrap with your own labelled text) to name what's loading;
  it's used as the bar's `aria-label`.
