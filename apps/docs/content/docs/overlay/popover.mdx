---
title: Popover
description: Popover — accessible, themeable, in React (@bpdm/ui) and Angular (@bpdm/ng).
---

The **Popover** component — available in React (`@bpdm/ui/popover`) and Angular (`@bpdm/ng`), from the same design tokens.

<Callout type="info">
  Live preview, usage code (React & Angular) and the auto-generated API table are
  being added to this page. For now, see it live in [Storybook](https://ui.bpdm.dev).
</Callout>
