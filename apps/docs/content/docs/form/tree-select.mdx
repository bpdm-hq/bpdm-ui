---
title: Tree Select
description: Tree Select — accessible, themeable, in React (@bpdm/ui) and Angular (@bpdm/ng).
---

The **Tree Select** component — available in React (`@bpdm/ui/tree-select`) and Angular (`@bpdm/ng`), from the same design tokens.

<Callout type="info">
  Live preview, usage code (React & Angular) and the auto-generated API table are
  being added to this page. For now, see it live in [Storybook](https://ui.bpdm.dev).
</Callout>
